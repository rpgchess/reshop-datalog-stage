/*
WScript.Network = {
    ComputaerName: string,
    UserDomain: String,
    UserName: string,
    AddWindowsPrinterConnection(printerPath: string),  // UNC path
    RemovePrinterConnection(printerPath: string, force: boolean, updateProfile: boolean), // force = true resource is removed, updateProfile = true printer mapping is removed
    MapNetworkDrive(localName: string, remoteName: string, updateProfile: boolean, user: string, password: string),
    RemoveNetworkDrive(name: string, force: boolean, updateProfile: boolean), // force = true resource is removed, updateProfile = true printer mapping is removed
}
*/